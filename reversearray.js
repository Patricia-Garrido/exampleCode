/*
function reverse (arr) {
   var arr = [0, 1, 2, 3, 4];
   for (var i = arr.length-1; i >= 0;i--) {
       rev = arr[i];
       console.log (arr[i])
   }
 }
 var x=reverse ();
 */


function reverse (word) {
   var arr = "cat";
   for (var i = arr-1; i >= 0;i--) {
       if (arr[i] == arr [i]);
       console.log (arr[i])
   }
 }
 var x=reverse ();